<?php
/**
 * Uninstall handler for the MSPress plugin.
 *
 * @package MSPress
 */

if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
    exit;
}

// The template intentionally leaves settings in place so deactivation is reversible.
